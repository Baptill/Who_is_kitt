<div class="content pending-screen" data-controller="pending" data-action="click->pending#playSound">
    <%= audio_tag("waiting.mp3", autoplay: true, :data => { :pending_target => 'audio'}) %>
    <div class="loading-pending mb-5">
      <h1 class="size70">En attente de votre adversaire</h1>

      <h3 class="text-center">Voici le lien du jeu 👇 <br><strong>www.whoiskitt.fun/games/<%= @game.id %>/</strong></h3>


      <div class="lds-ellipsis"><div></div><div></div><div></div><div></div></div>
    </div>

  <div class="user-pending">
    <div class="item-playeur">
      <div class="current-avatar">
        <% if current_user.photo.key? %>
              <%= cl_image_tag current_user.photo.key, crop: :fill %>
        <% end %>
      </div>
      <span><h2 style="color:#CE001D !important"><%= current_user.nickname %></h2></span>
    </div>
    <h1 class="vs fight-catchline" data-pending-target="vs">VS</h1>
    <div class="item-playeur">
      <div class="current-avatar">
        <%= image_tag "waitingpic.gif", width:"100px", style:"object-fit: cover" %>
      </div>
      <span><h2>Joueur 2</h2></span>
    </div>
  </div>
</div>
